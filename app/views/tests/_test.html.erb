<tr>
  <td><%= test.title %></td>
  <td><%= test.questions.count %></td>
  <td>
    <%= link_to 'Просмотр', test_path(test) %>
    <%= link_to 'Редактировать', edit_test_path(test) %>
    <%= link_to 'Удалить', test_path(test), method: :delete, data: { confirm: 'Are you sure?' } %>
  </td>
</tr>
