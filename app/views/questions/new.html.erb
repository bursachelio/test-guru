<h1>Create new question</h1>

<%= form_for [@test, @question] do |f| %>
  <div class="field">
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit 'Create Question' %>
  </div>
<% end %>
